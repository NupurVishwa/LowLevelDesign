package import ja

/*
 * Enum represents the symbol of each player.
 * Instead of using String ("X"), we use Enum because
 * it is type-safe and prevents invalid symbols.
 */

public enum Symbol {

    X,
    O
}