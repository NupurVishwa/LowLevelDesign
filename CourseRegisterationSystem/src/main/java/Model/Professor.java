package Model;

public record Professor(String id, String name) {
    // Defines a record named Professor.
    // A record is used to store simple, immutable data.


    // 'id' stores the unique ID of the professor.
    // 'name' stores the professor's name.
}